{
  "name": "bd-sms-aggregator",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/api",
    "apps/worker",
    "apps/web",
    "packages/db"
  ],
  "scripts": {
    "build": "npm run build -w apps/api && npm run build -w apps/web && npm run build -w apps/worker"
  }
}
