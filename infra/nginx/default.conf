upstream sms_api { server api:3000; } server { listen 80; server_name _; client_max_body_size 25m; location /api/ { proxy_pass http://sms_api; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; } location / { root /usr/share/nginx/html; try_files $uri /index.html; } }
