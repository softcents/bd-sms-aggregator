import { Pool } from 'pg'; export const db=new Pool({connectionString:process.env.DATABASE_URL,max:Number(process.env.WORKER_DB_POOL||10)}); export async function markSent(externalId:string,gatewayMessageId:string,report:any){await db.query('UPDATE "Message" SET status=$1,"gatewayMessageId"=$2,"sentAt"=NOW(),"infozillionReport"=$3,"nextPollAt"=NOW(),"updatedAt"=NOW() WHERE "externalId"=$4 AND status IN ($5,$6)',['sent',gatewayMessageId,JSON.stringify(report),externalId,'queued','pending']);} export async function markFailed(externalId:string,reason:string,report:any){await db.query('UPDATE "Message" SET status=$1,"failedReason"=$2,"failedAt"=NOW(),"infozillionReport"=$3,"nextPollAt"=NULL,"updatedAt"=NOW() WHERE "externalId"=$4 AND status NOT IN ($5,$6)',['failed',reason,JSON.stringify(report),externalId,'delivered','failed']);}