import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import './style.css';
const API=import.meta.env.VITE_API_URL||''; const uid=localStorage.getItem('userId')||'1'; const headers={'content-type':'application/json','x-user-id':uid};
function App(){const [summary,setSummary]=useState<any>(null);const [messages,setMessages]=useState<any[]>([]);const [to,setTo]=useState('');const [sender,setSender]=useState('');const [body,setBody]=useState('');const [msg,setMsg]=useState('');async function load(){const [a,b]=await Promise.all([fetch(API+'/reports/summary',{headers}),fetch(API+'/reports/messages?limit=20',{headers})]);setSummary(await a.json());setMessages(await b.json())}useEffect(()=>{load()},[]);async function send(e:any){e.preventDefault();setMsg('Sending...');const r=await fetch(API+'/sms/send',{method:'POST',headers,body:JSON.stringify({senderId:sender,to:to.split(/[,\\s]+/).filter(Boolean),body,isUnicode:/[^\\x00-\\x7F]/.test(body)})});const d=await r.json();setMsg(r.ok?'SMS queued: '+d.recipients+' recipients':(d.message||'Send failed'));if(r.ok){setTo('');setBody('');load()}}return <div className="app"><header><h1>SMS Gateway</h1><div className="balance">Balance: ৳{summary?.balance??'—'}</div></header><main><section className="cards">{[['Total',summary?.total],['Queued',summary?.queued],['Sent',summary?.sent],['Delivered',summary?.delivered],['Failed',summary?.failed],['Cost',summary?.total_cost]].map(x=><div className="card"><small>{x[0]}</small><strong>{x[1]??'—'}</strong></div>)}</section><section className="grid"><form className="panel" onSubmit={send}><h2>Send SMS</h2><label>Sender ID<input value={sender} onChange={e=>setSender(e.target.value)} required/></label><label>Recipients<textarea value={to} onChange={e=>setTo(e.target.value)} placeholder="8801XXXXXXXXX, 8801XXXXXXXXX" required/></label><label>Message<textarea value={body} onChange={e=>setBody(e.target.value)} required/></label><button>Send SMS</button><p>{msg}</p></form><section className="panel"><h2>Recent Messages</h2><div className="table">{messages.map(m=><div className="row"><span>{m.to}</span><span>{m.status}</span><span>৳{m.cost}</span></div>)}</div></section></section></main></div>}createRoot(document.getElementById('root')!).render(<App/>);