import { CanActivate, ExecutionContext, Injectable, UnauthorizedException } from '@nestjs/common'; import { createHash, timingSafeEqual } from 'crypto';
@Injectable() export class ApiKeyGuard implements CanActivate { canActivate(ctx:ExecutionContext){const req=ctx.switchToHttp().getRequest();const supplied=String(req.headers['x-api-key']||'');const expected=process.env.API_MASTER_KEY||'';if(!supplied||!expected)throw new UnauthorizedException('API key required');const a=createHash('sha256').update(supplied).digest(),b=createHash('sha256').update(expected).digest();if(!timingSafeEqual(a,b))throw new UnauthorizedException('Invalid API key');return true;} }
