import { Body, Controller, Headers, Post } from '@nestjs/common'; import { SmsService } from './sms.service'; import { SendSmsDto } from './dto/send-sms.dto'; @Controller('sms') export class SmsController { constructor(private readonly sms:SmsService){} @Post('send') send(@Body() dto:SendSmsDto,@Headers('x-user-id') userId?:string){return this.sms.send(dto,userId)} }