import { Body, Controller, Post, Req, UseGuards } from '@nestjs/common'; import { SmsService } from './sms.service'; import { SendSmsDto } from './dto/send-sms.dto'; import { ApiKeyGuard } from '../auth/api-key.guard'; @Controller('sms') export class SmsController { constructor(private readonly sms:SmsService){} @UseGuards(ApiKeyGuard) @Post('send') send(@Body() dto:SendSmsDto,@Req() req:any){return this.sms.send(dto,req.userId)} }