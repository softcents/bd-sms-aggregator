import { Injectable } from '@nestjs/common'; import { PrismaClient } from '@prisma/client'; @Injectable() export class MessageRepository { private db=new PrismaClient(); async createQueued(data:any){return this.db.message.create({data:{userId:data.userId,batchId:data.batchId,senderId:data.senderId,gatewayId:data.gatewayId,to:data.to,body:data.body,status:'queued',cost:data.cost||0,rate:data.rate||0,parts:data.parts||1,isUnicode:!!data.isUnicode}});} async markSent(id:bigint,gatewayMessageId:string,report:any){return this.db.message.update({where:{id},data:{status:'sent',gatewayMessageId,sentAt:new Date(),infozillionReport:report,nextPollAt:new Date()}});} async markDelivered(id:bigint,report:any){return this.db.message.update({where:{id},data:{status:'delivered',deliveredAt:new Date(),infozillionReport:report,nextPollAt:null}});} async markFailed(id:bigint,reason:string,report?:any){return this.db.message.update({where:{id},data:{status:'failed',failedAt:new Date(),failedReason:reason,infozillionReport:report,nextPollAt:null}});} }